(let enter "<br>")

(function max (params a b c)
    (block
        (let bigger a)
        (if (> b bigger) (let bigger b))
        (if (> c bigger) (let bigger c))
        (return bigger)
    )
)

(print (max 5 8 11) enter)
